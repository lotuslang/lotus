var greeting = "hi";
var name = "Mark";

print(greeting + ' ' + name + '!');

greeting = "hello";
name = "world";

print(greeting + " " + name);

def printError(msg, tk) {
    var location = tk;

    msg.toStr(h, "\x").reverse();
    print("({location}) : {msg}");
}

printError("un", tk);